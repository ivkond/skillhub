---
phase: 01-domain-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillReadableForActorPort.java
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionLimits.java
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionAuthorizationPolicy.java
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainService.java
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionContributorService.java
  - server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainServiceTest.java
  - server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipServiceTest.java
  - server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionContributorServiceTest.java
  - server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionAuthorizationPolicyTest.java
autonomous: true
requirements:
  - COL-01
  - COL-02
  - COL-03
  - COL-04
  - COL-05
  - COL-06
  - COL-07
  - ROL-01
  - ROL-02
  - ROL-03
  - ROL-04
  - ROL-05
  - ROL-06
  - ROL-07
  - ROL-08
must_haves:
  truths:
    - "Owner (or admin-equivalent per **ROL-08** / **D-12**) can create/update metadata/delete collection; contributor cannot metadata/visibility/delete/manage contributors (**ROL-07**)."
    - "Add skill to collection calls `SkillReadableForActorPort`; false rejects with domain bad request (**COL-04**, **ROL-04**, **INT-01**)."
    - "Caps enforced: 100 skills per collection, 50 collections per owner, 20 contributors per collection (REQUIREMENTS limits)."
    - "Per-owner slug collision throws domain validation error, not silent truncate (**D-06**)."
    - "Reorder persists `sort_order` batch under `@Transactional` with stable ordering `(sort_order ASC, id ASC)` (**COL-06**, **ROL-06**, **D-08**)."
  artifacts:
    - path: server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillReadableForActorPort.java
      provides: "Port for skill readability by acting user"
      exports: ["SkillReadableForActorPort"]
    - path: server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionAuthorizationPolicyTest.java
      provides: "Executable policy matrix tests"
      min_lines: 40
  key_links:
    - from: SkillCollectionMembershipService
      to: SkillReadableForActorPort
      via: "method call before insert member"
      pattern: "SkillReadableForActorPort"
    - from: SkillCollectionDomainService
      to: SkillCollectionRepository
      via: "countByOwnerId / existsByOwnerIdAndSlug"
      pattern: "countByOwnerId|existsByOwnerIdAndSlug"
---

<objective>
Implement **domain services** for skill collections: CRUD on the aggregate root, contributor grant/revoke, membership add/remove/reorder with caps, **authorization policy** (owner vs contributor vs admin-equivalent **ROL-08**), **per-owner slug collision** handling (**D-06**), and a **mocked** `SkillReadableForActorPort` in unit tests for **COL-04** / **ROL-04** / **INT-01**—without HTTP controllers (**01-CONTEXT** phase boundary).

Purpose: Delivers ROADMAP Phase 1 success criteria (domain APIs + unit-tested policy) so Phase 2 can wire REST and real visibility adapters.

Output: Port interface, limits/constants, `SkillCollectionAuthorizationPolicy`, three `@Service` (or equivalent) classes for collection / membership / contributor flows, and JUnit 5 + Mockito tests with table-driven cases for **ROL-07** and **ROL-08**.
</objective>

<execution_context>
@.planning/phases/01-domain-persistence/01-CONTEXT.md
@.planning/phases/01-domain-persistence/01-RESEARCH.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/shared/exception/DomainBadRequestException.java
@server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/namespace/NamespaceMemberServiceTest.java
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionRepository.java
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMemberRepository.java
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionContributorRepository.java
@CLAUDE.md

<interfaces>
Repository port method names from plan **01-01** (executor must align implementations if code drifted): `SkillCollectionRepository.countByOwnerId`, `existsByOwnerIdAndSlug`, `findById`, `save`, `delete`; `SkillCollectionMemberRepository.findByCollectionIdOrderBySortOrderAscIdAsc` (or query equivalent), `countByCollectionId`, `deleteByCollectionIdAndSkillId`, bulk save for reorder; `SkillCollectionContributorRepository.existsByCollectionIdAndUserId`, `countByCollectionId`, `deleteByCollectionIdAndUserId`, `findByCollectionId`, `save`.

`SkillReadableForActorPort` contract (name locked for traceability to **D-10**): single boolean method, e.g. `boolean canActingUserReadSkill(String actingUserId, long skillId)` — **no** duplicated portal visibility rules in services; only this port answers readability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port, limits, authorization policy (D-12 / ROL-08)</name>
  <read_first>
    - `.planning/phases/01-domain-persistence/01-CONTEXT.md` — **D-10**, **D-12**, **ROL-08**; **ROL-07** contributor prohibitions.
    - `.planning/REQUIREMENTS.md` — contributor vs owner matrix; caps **100 / 50 / 20**.
    - `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/shared/exception/DomainBadRequestException.java` — throw style for validation failures and slug collisions.
  </read_first>
  <action>
    Add **`SkillReadableForActorPort`** in `com.iflytek.skillhub.domain.collection` with the boolean readability method described in `<interfaces>` (**COL-04**, **ROL-04**, **INT-01**).

    Add **`SkillCollectionLimits`** (final class or interface + enum) with **public static final** (or records) integers: `MAX_SKILLS_PER_COLLECTION = 100`, `MAX_COLLECTIONS_PER_OWNER = 50`, `MAX_CONTRIBUTORS_PER_COLLECTION = 20` sourced from **REQUIREMENTS.md** (tunable constants; single source of truth for services).

    Add **`SkillCollectionAuthorizationPolicy`** with an explicit operation enum, e.g. `CollectionOperation { CREATE_COLLECTION, UPDATE_METADATA, DELETE_COLLECTION, SET_VISIBILITY, ADD_REMOVE_CONTRIBUTOR, ADD_SKILL, REMOVE_SKILL, REORDER_SKILLS }` (adjust names to fit codebase style) and method `void checkAllowed(boolean ownerActor, boolean contributorActor, boolean adminEquivalent, CollectionOperation op)` throwing `DomainBadRequestException` or a dedicated unchecked domain security exception **only if** an existing project type is preferred—otherwise use `DomainBadRequestException` with stable message codes as strings for tests.

    Policy rules (**D-12**): `adminEquivalent == true` ⇒ treat as owner for **all** operations (**ROL-08**). Contributor allowed: **ADD_SKILL**, **REMOVE_SKILL**, **REORDER_SKILLS** only. Contributor denied: metadata, visibility, delete collection, contributor management (**ROL-07**). Owner allowed for all listed operations. `ownerActor` and `contributorActor` are **mutually exclusive** classifications passed from caller (Phase 2 classifies); if neither owner nor contributor nor admin, deny mutations.
  </action>
  <verify>
    <automated><![CDATA[cd server && ./mvnw -q -pl skillhub-domain test -Dtest=SkillCollectionAuthorizationPolicyTest]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "interface SkillReadableForActorPort" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillReadableForActorPort.java` exits 0.
    - `rg -n "MAX_SKILLS_PER_COLLECTION|100" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionLimits.java` exits 0.
    - `rg -n "SkillCollectionAuthorizationPolicy" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionAuthorizationPolicy.java` exits 0.
    - `cd server && ./mvnw -q -pl skillhub-domain test -Dtest=SkillCollectionAuthorizationPolicyTest` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Domain services — collection CRUD, membership, contributors, slug and caps</name>
  <read_first>
    - Repository interfaces and entities from plan **01-01** `files_modified` list.
    - `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/skill/SkillVisibility.java` — validate visibility inputs on create/update.
    - `server/skillhub-app/src/main/resources/db/migration/V4__normalize_skill_slugs.sql` — mirror slug normalization validation in Java (trim, length, charset) before persist; on conflict `existsByOwnerIdAndSlug` ⇒ `DomainBadRequestException` (**D-06**).
    - `SkillCollectionAuthorizationPolicy` + `SkillCollectionLimits` from Task 1.
  </read_first>
  <action>
    Implement **`SkillCollectionDomainService`** (annotated `@Service` if project uses Spring in domain module for services—match `NamespaceMemberService`; if domain services are plain POJO, match existing collection-less pattern):

    - **`createCollection(...)`** (signature chosen by executor): parameters include `actingUserId`, `title`, `description`, `visibility`, `slug`, boolean `adminEquivalent`, and optional `targetOwnerId` used **only** when `adminEquivalent` is true for support flows; for normal users `ownerId = actingUserId`. Enforce `countByOwnerId(ownerId) &lt; MAX_COLLECTIONS_PER_OWNER`; slug validate + `existsByOwnerIdAndSlug` ⇒ `DomainBadRequestException` (**COL-01**, **COL-07**, caps, **ROL-08**).

    - **updateMetadata(...)`, `setVisibility(...)`, `deleteCollection(...)`** with policy checks (**COL-02**, **COL-03**, **ROL-07**). Delete uses `repository.delete` relying on DB CASCADE for members/contributors (**D-03**).

    Implement **`SkillCollectionMembershipService`**:

    - **addSkill(collectionId, actingUserId, skillId, flags...)**: policy allows owner or contributor; call `SkillReadableForActorPort.canActingUserReadSkill(actingUserId, skillId)`—false ⇒ `DomainBadRequestException` (**COL-04**, **ROL-04**, **INT-01**); enforce `countByCollectionId &lt; MAX_SKILLS_PER_COLLECTION`; reject duplicate skill if repo `exists` or catch unique constraint—prefer proactive check + `DomainBadRequestException` (**INT-02**).

    - **removeSkill(...)** (**COL-05**, **ROL-05**) with policy.

    - **reorderSkills(collectionId, actingUserId, orderedSkillIds)`**: verify list matches existing membership set (same elements, no extras); batch update `sort_order` starting from 0 or gaps allowed (**D-09**); use `@Transactional` on service class or methods; order reads `(sort_order ASC, id ASC)` (**COL-06**, **ROL-06**, **D-08**).

    Implement **`SkillCollectionContributorService`**:

    - **addContributor** / **removeContributor** — policy: **only owner or admin-equivalent** (**ROL-02**, **ROL-03**, **ROL-07**); enforce `countByCollectionId &lt; MAX_CONTRIBUTORS_PER_COLLECTION`; reject duplicate `existsByCollectionIdAndUserId`.

    Services take repository ports + policy + port dependency via constructor injection pattern used in `NamespaceMemberService`.

    **No** visibility rule copy-paste beyond port usage (**D-10**).

    Per **CLAUDE.md**, run GitNexus impact on any **modified existing** service base classes; new services are new symbols.
  </action>
  <verify>
    <automated><![CDATA[cd server && ./mvnw -q -pl skillhub-domain test -Dtest=SkillCollectionDomainServiceTest,SkillCollectionMembershipServiceTest,SkillCollectionContributorServiceTest,SkillCollectionAuthorizationPolicyTest]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class SkillCollectionDomainService" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainService.java` exits 0.
    - `rg -n "SkillReadableForActorPort" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java` exits 0.
    - `rg -n "@Transactional" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java` exits 0 (reorder transaction).
    - `cd server && ./mvnw -q -pl skillhub-domain test -Dtest=SkillCollectionDomainServiceTest,SkillCollectionMembershipServiceTest,SkillCollectionContributorServiceTest,SkillCollectionAuthorizationPolicyTest` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Unit tests — matrix, mocked port, slug collision, caps</name>
  <read_first>
    - `server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/namespace/NamespaceMemberServiceTest.java` — Mockito patterns.
    - Services and policy from Tasks 1–2.
  </read_first>
  <action>
    Add **`SkillCollectionDomainServiceTest`**, **`SkillCollectionMembershipServiceTest`**, **`SkillCollectionContributorServiceTest`** (split files if >500 lines combined—executor chooses) with **Mockito** `@ExtendWith(MockitoExtension.class)` mocking `SkillCollectionRepository`, member/contributor repos, `SkillReadableForActorPort`, and policy if not under test.

    Required scenarios (names as `@DisplayName` or method names):

    - **COL-04 / ROL-04 / INT-01**: port returns `false` ⇒ add skill throws `DomainBadRequestException`; port `true` ⇒ persists when under cap.
    - **ROL-07**: contributor throws on `updateMetadata`, `setVisibility`, `deleteCollection`, `addContributor`.
    - **ROL-08**: `adminEquivalent=true` non-owner passes owner-only operations in policy tests and domain service entry (pass explicit flag through service methods as designed in Task 2).
    - **D-06**: `existsByOwnerIdAndSlug` true ⇒ create or slug update throws.
    - **Caps**: 101st skill, 51st collection, 21st contributor ⇒ `DomainBadRequestException`.
    - **COL-06 / ROL-06**: reorder updates `sort_order` on mocked `saveAll` or individual saves—verify invoked sequence.

    Do **not** add `@WebMvcTest` or controllers.

    After domain tests green, run **`make test-backend`** from repo root as final regression (depends on **01-01** already merged in branch for Flyway—if this plan executes alone after 01-01, full suite still required before phase sign-off).
  </action>
  <verify>
    <automated><![CDATA[make test-backend]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SkillReadableForActorPort" server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection` exits 0 (mock field or constructor param).
    - `rg -n "UPDATE_METADATA|contributor" server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection` exits 0 (evidence of **ROL-07** negative coverage).
    - `make test-backend` from repo root exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain service entry | `actingUserId`, role flags, and `adminEquivalent` are **trusted only as far as Phase 2 HTTP layer** will validate; Phase 1 must never infer identity from entity `ownerId` alone without caller-supplied context (**D-12**). |
| Skill readability | Crosses into skill visibility subsystem via **port** only (**D-10**). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Test hook |
|-----------|----------|-----------|-------------|-----------------|-----------|
| T-01-02-01 | Elevation | `SkillCollectionDomainService` | mitigate | `SkillCollectionAuthorizationPolicy` denies contributor for owner-only `CollectionOperation` values (**ROL-07**). | `SkillCollectionDomainServiceTest` contributor-negative cases. |
| T-01-02-02 | Elevation | Admin bypass | mitigate | Explicit `adminEquivalent` (or `ownerIsAdminEquivalent`) path treated as owner (**ROL-08**, **D-12**). | Policy + service tests with admin flag true. |
| T-01-02-03 | Information disclosure | Add skill | mitigate | Call `SkillReadableForActorPort` before insert; false rejects add (**COL-04**, **INT-01**). | Mockito stub `false` / `true` tests. |
| T-01-02-04 | Tampering | Duplicate membership | mitigate | Pre-check + DB unique from **01-01**; map to `DomainBadRequestException`. | Test duplicate add throws. |
| T-01-02-05 | Denial of service | Reorder huge lists | accept | Cap 100 skills bounds workload; document O(n) save. | Membership cap tests. |
</threat_model>

<verification>
- `cd server && ./mvnw -pl skillhub-domain test` passes for new test classes.
- `make test-backend` passes at end of Task 3 (full CI-equivalent gate).
</verification>

<success_criteria>
1. Domain services implement **COL-01**–**COL-07** and **ROL-01**–**ROL-08** at the service layer per **REQUIREMENTS.md** and **01-CONTEXT.md** locked decisions.
2. `SkillReadableForActorPort` is the **only** skill-readability gate for add-skill (**D-10**); unit tests mock it.
3. Authorization matrix and caps are covered by automated tests; `make test-backend` is green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-persistence/01-02-SUMMARY.md` per execute-plan workflow.
</output>
