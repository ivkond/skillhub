---
phase: 05-ux-add-visible-skills
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/shared/hooks/use-collection-queries.ts
  - web/src/shared/hooks/use-collection-queries.test.ts
  - web/src/shared/hooks/use-collection-add-candidates.ts
autonomous: true
requirements:
  - COL-04
  - ROL-04
  - INT-01
  - VIS-03
  - WEB-03
must_haves:
  truths:
    - "Add-flow data source returns skills visible to acting user with search/filter/sort/pagination controls."
    - "Batch add action preserves server-side authorization and duplicate rejection by reusing existing single-add API."
    - "Collection detail cache is invalidated after each successful add so UI reflects latest membership immediately."
  artifacts:
    - path: web/src/shared/hooks/use-collection-queries.ts
      provides: "React Query hooks for candidate discovery and multi-select add orchestration."
      contains: "useCollectionAddCandidates"
    - path: web/src/shared/hooks/use-collection-queries.test.ts
      provides: "Behavioral tests proving query keys, API calls, and invalidation behavior."
      contains: "useBulkAddCollectionSkills"
  key_links:
    - from: web/src/shared/hooks/use-collection-add-candidates.ts
      to: web/src/shared/hooks/use-collection-queries.ts
      via: "Candidate mapper integration for useCollectionAddCandidates."
    - from: web/src/shared/hooks/use-collection-queries.ts
      to: web/src/api/client.ts
      via: "useBulkAddCollectionSkills calls collectionApi.addSkill for each selected skill."
    - from: web/src/shared/hooks/use-collection-queries.test.ts
      to: web/src/shared/hooks/use-collection-queries.ts
      via: "Hook tests assert batch add classification and cache invalidation contract."
---

# Phase 5 Plan 01: Add-Flow Data Layer And Mutation Orchestration

<objective>
Build the reusable data and mutation layer for the new collection add-flow so UI can discover visible skills and submit multi-select add operations safely.

Scope includes new hooks, query-key strategy, and TDD coverage for batch add behavior.
</objective>

<context>
@.planning/phases/05-ux-add-visible-skills/05-CONTEXT.md
@.planning/phases/05-ux-add-visible-skills/05-RESEARCH.md
@.planning/REQUIREMENTS.md
@web/src/shared/hooks/use-collection-queries.ts
@web/src/shared/hooks/use-collection-queries.test.ts
@web/src/shared/hooks/use-skill-queries.ts
@web/src/shared/hooks/skill-query-helpers.ts
@web/src/api/client.ts
</context>

<threat_model>
<asvs_level>1</asvs_level>
<threats>
  <threat id="T-05-01-01" severity="high">Candidate query exposes skills not visible to acting user via alternate endpoint or client-side filtering mistakes.</threat>
  <threat id="T-05-01-02" severity="medium">Batch add flow bypasses server authorization/duplicate checks by trying to synthesize membership locally.</threat>
  <threat id="T-05-01-03" severity="medium">Stale cache after add allows user to submit duplicate add actions repeatedly and receive avoidable failures.</threat>
</threats>
<mitigations>
  <item>Use existing visibility-safe `/api/web/skills` query path through `useSearchSkills` primitives instead of ad-hoc endpoints.</item>
  <item>Implement batch as repeated calls to existing `collectionApi.addSkill` endpoint; server remains source of truth for `COL-04` and `INT-01`.</item>
  <item>Invalidate `['collections', id]` and `['collections', 'mine']` after successful additions.</item>
</mitigations>
<block_on>high</block_on>
</threat_model>

<tasks>
<task type="test">
  <name>Task 1: Add failing hook tests for candidate query and batch add orchestration</name>
  <files>
    web/src/shared/hooks/use-collection-queries.test.ts
    web/src/shared/hooks/use-collection-queries.ts
    web/src/shared/hooks/use-skill-queries.ts
    web/src/shared/hooks/skill-query-helpers.ts
  </files>
  <read_first>
    @web/src/shared/hooks/use-collection-queries.test.ts
    @web/src/shared/hooks/use-collection-queries.ts
    @web/src/shared/hooks/use-skill-queries.ts
    @web/src/shared/hooks/skill-query-helpers.ts
  </read_first>
  <action>
    Extend `web/src/shared/hooks/use-collection-queries.test.ts` with failing tests that define required behavior:
    1) `useCollectionAddCandidates` must use a stable query key containing collection id, search text, label, sort, page, and size.
    2) Candidate query must call existing search pipeline (`collectionApi` or helper abstraction selected in implementation) without bypassing visibility rules.
    3) `useBulkAddCollectionSkills` must call `collectionApi.addSkill` once per selected skill id and return a structured result (`addedIds`, `alreadyInCollectionIds`, `failedIds`).
    4) On at least one success, mutation must invalidate `['collections', 'mine']` and `['collections', id]`.
  </action>
  <acceptance_criteria>
    - `web/src/shared/hooks/use-collection-queries.test.ts` contains `describe('useCollectionAddCandidates'`.
    - `web/src/shared/hooks/use-collection-queries.test.ts` contains `describe('useBulkAddCollectionSkills'`.
    - A test asserts `collectionApi.addSkill` call count equals selected id count.
    - A test asserts invalidation call for query key `['collections', '42']`.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[cd web && pnpm run test -- src/shared/hooks/use-collection-queries.test.ts]]></automated>
  </verify>
  <done>Hook tests fail before implementation and pass after implementation, with explicit assertions for query key and batch classification behavior.</done>
</task>

<task type="code">
  <name>Task 2: Implement collection add candidate query hook and batch add mutation</name>
  <files>
    web/src/shared/hooks/use-collection-queries.ts
    web/src/shared/hooks/use-skill-queries.ts
    web/src/shared/hooks/skill-query-helpers.ts
  </files>
  <read_first>
    @web/src/shared/hooks/use-collection-queries.ts
    @web/src/shared/hooks/use-skill-queries.ts
    @web/src/shared/hooks/skill-query-helpers.ts
    @web/src/api/client.ts
  </read_first>
  <action>
    Implement concrete APIs in the collection hooks layer:
    - Add `useCollectionAddCandidates(params)` that accepts `{ collectionId, q, label, sort, page, size }` and returns paged candidate skills using existing visibility-safe search primitives.
    - Add `useBulkAddCollectionSkills()` mutation that accepts `{ id, skillIds }`, executes `collectionApi.addSkill(id, skillId)` per id, classifies errors (`error.skillCollection.member.duplicate` into `alreadyInCollectionIds`), and returns a deterministic result object.
    - Ensure mutation invalidates collection detail and mine queries only when at least one add succeeds.
    - Keep existing hooks backward-compatible; no breaking signature changes for current consumers.
  </action>
  <acceptance_criteria>
    - `web/src/shared/hooks/use-collection-queries.ts` exports `useCollectionAddCandidates`.
    - `web/src/shared/hooks/use-collection-queries.ts` exports `useBulkAddCollectionSkills`.
    - `useBulkAddCollectionSkills` return type includes keys `addedIds`, `alreadyInCollectionIds`, and `failedIds`.
    - Existing `useAddCollectionSkill` export remains available for backward compatibility.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[cd web && pnpm run test -- src/shared/hooks/use-collection-queries.test.ts]]></automated>
  </verify>
  <done>Collection hooks export candidate and batch-add APIs with backward-compatible signatures and passing tests.</done>
</task>

<task type="code">
  <name>Task 3: Normalize candidate helper module for reuse by modal UI</name>
  <files>
    web/src/shared/hooks/use-collection-add-candidates.ts
    web/src/shared/hooks/use-collection-queries.ts
  </files>
  <read_first>
    @web/src/shared/hooks/use-collection-queries.ts
    @web/src/shared/hooks/skill-query-helpers.ts
    @web/src/shared/hooks/use-skill-queries.ts
  </read_first>
  <action>
    Add a focused helper module `web/src/shared/hooks/use-collection-add-candidates.ts` (or an equivalent concrete file name) that maps raw search response into picker candidate view model:
    - includes `id`, `displayName`, `namespace`, `summary`, `visibility`, `status`.
    - computes `alreadyInCollection` based on provided member skill-id set.
    - exposes pure mapping function suitable for unit tests.
  </action>
  <acceptance_criteria>
    - `web/src/shared/hooks/use-collection-add-candidates.ts` exists.
    - File exports mapping function containing the string `alreadyInCollection`.
    - `useCollectionAddCandidates` imports and uses the new mapper file.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[cd web && pnpm run test -- src/shared/hooks/use-collection-queries.test.ts]]></automated>
  </verify>
  <done>Candidate mapping helper exists and is used by collection candidate query path with deterministic already-in-collection flag.</done>
</task>
</tasks>

<verification>
- `cd web && pnpm run test -- src/shared/hooks/use-collection-queries.test.ts`
- `cd web && pnpm run typecheck`
</verification>

<success_criteria>
- Candidate data/mutation hooks are production-ready and consumed by downstream UI tasks.
- Batch add orchestrates existing server contract without introducing new permission bypass.
- Hook-level TDD tests pass and document expected behavior for future regressions.
</success_criteria>
