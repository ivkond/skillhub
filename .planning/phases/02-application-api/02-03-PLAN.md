---
phase: 02-application-api
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java
  - server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillCollectionPortalCommandAppService.java
  - server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/portal/SkillCollectionController.java
  - server/skillhub-app/src/main/java/com/iflytek/skillhub/schedule/SkillCollectionReconciliationScheduler.java
  - server/skillhub-app/src/main/resources/application.yml
  - server/skillhub-app/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipReconcileTest.java
autonomous: true
requirements:
  - INT-03
  - ADM-01
must_haves:
  truths:
    - "A defined reconciliation path removes or flags collection member rows for skills the **collection owner** can no longer read via `SkillReadableForActorPort`, executed periodically and covered by an automated test with mocked port behavior."
    - "Admin-equivalent destructive or visibility-changing operations on collections emit an `AuditLogService` record with stable action codes (e.g. `SKILL_COLLECTION_ADMIN_DELETE`)."
  artifacts:
    - path: server/skillhub-app/src/main/java/com/iflytek/skillhub/schedule/SkillCollectionReconciliationScheduler.java
      provides: "Scheduled INT-03 reconciliation"
      contains: "@Scheduled"
    - path: server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java
      provides: "Reconcile method"
      contains: "reconcile"
  key_links:
    - from: SkillCollectionMembershipService
      to: SkillReadableForActorPort
      via: "canActingUserReadSkill(collection.getOwnerId(), skillId)"
      pattern: "canActingUserReadSkill"
---

<objective>
Close **INT-03** with an **automated reconciliation** strategy for stale skill memberships and implement **ADM-01** audit records for **admin-equivalent** collection mutations, without weakening Phase 1 domain invariants.
</objective>

<threat_model>
**ASVS L1**

| Risk | Mitigation |
|------|------------|
| Reconciliation deletes legitimate rows | Only delete when `SkillReadableForActorPort.canActingUserReadSkill(ownerId, skillId) == false` after explicit null-checks; unit test with controlled doubles. |
| Audit log injection | Pass structured JSON fragments with escaped identifiers only; reuse `AuditLogService.record` signature from `SkillLabelAppService`. |
| Scheduler overload | Batch by collection id with pagination (`findAll` limited page) — document max batch size constant **100** per tick. |
</threat_model>

<execution_context>
@.planning/phases/02-application-api/02-RESEARCH.md
@.planning/REQUIREMENTS.md — **INT-03**, **ADM-01**
</execution_context>

<context>
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillReadableForActorPort.java
@server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillLabelAppService.java
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/audit/AuditLogService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain reconcile method on SkillCollectionMembershipService</name>
  <read_first>
    - `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java`
    - `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionRepository.java`
    - `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillReadableForActorPort.java`
  </read_first>
  <action>
    Add **`@Transactional` public int reconcileInvisibleSkillsForCollection(long collectionId)`** to `SkillCollectionMembershipService` that:

    1. Loads `SkillCollection` via `skillCollectionRepository.findById(collectionId)`; if empty return `0`.
    2. Loads all `SkillCollectionMember` rows via `memberRepository.findByCollectionIdOrderBySortOrderAscIdAsc(collectionId)`.
    3. For each member, if `!skillReadableForActorPort.canActingUserReadSkill(collection.getOwnerId(), member.getSkillId())`, call `memberRepository.deleteByCollectionIdAndSkillId(collectionId, member.getSkillId())`.
    4. Returns count of deleted rows (int).

    Do **not** change `addSkill` / `removeSkill` contracts.

    Method name must be exactly `reconcileInvisibleSkillsForCollection` for traceability in tests and ops docs.
  </action>
  <verify>
    <automated><![CDATA[cd server && ./mvnw -q -pl skillhub-domain -DskipTests compile]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "reconcileInvisibleSkillsForCollection" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java` exits 0.
    - `./mvnw -q -pl skillhub-domain -DskipTests compile` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Unit test SkillCollectionMembershipReconcileTest</name>
  <read_first>
    - `server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipServiceTest.java` — Mockito setup style.
  </read_first>
  <action>
    Create `server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipReconcileTest.java` with tests:

    1. When port returns **false** for a member skill, `reconcileInvisibleSkillsForCollection` deletes that row and returns `1` (mock repositories with saved collection + single member).
    2. When port returns **true**, returns `0` and `deleteByCollectionIdAndSkillId` never called (`verify(..., never())`).

    Use `@ExtendWith(MockitoExtension.class)` consistent with sibling tests in the same package.
  </action>
  <verify>
    <automated><![CDATA[cd server && ./mvnw -q -pl skillhub-domain -Dtest=SkillCollectionMembershipReconcileTest test]]></automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -q -pl skillhub-domain -Dtest=SkillCollectionMembershipReconcileTest test` exits 0.
    - `rg -n "reconcileInvisibleSkillsForCollection" server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipReconcileTest.java` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SkillCollectionReconciliationScheduler + config</name>
  <read_first>
    - `server/skillhub-app/src/main/resources/application.yml` — top-level custom property style if any `skillhub.*` keys exist (`rg -n "skillhub" server/skillhub-app/src/main/resources/application.yml`).
    - Search `server/skillhub-app/src/main/java` for existing `@Scheduled` usage to mirror enablement (`@EnableScheduling` location).
  </read_first>
  <action>
    1. Add `skillhub.collections.reconcile-cron` property default **`0 0 * * * *`** (hourly) under a documented `skillhub:` YAML section in `server/skillhub-app/src/main/resources/application.yml`.
    2. Create `server/skillhub-app/src/main/java/com/iflytek/skillhub/schedule/SkillCollectionReconciliationScheduler.java` as `@Component` with constructor-injected `SkillCollectionRepository` + `SkillCollectionMembershipService`.
    3. Method `@Scheduled(cron = "${skillhub.collections.reconcile-cron}")` **`public void reconcileAllCollections()`** that:
       - Reads collection ids in pages of **100** (`PageRequest.of(page,100)`) until empty.
       - Calls `membershipService.reconcileInvisibleSkillsForCollection(id)` for each id.
    4. Ensure scheduling is active in the Spring Boot app (if `@EnableScheduling` is not already on `SkillhubApplication`, add it there — only one `@EnableScheduling` for the app).

    Cron expression string must appear verbatim as `0 0 * * * *` in `application.yml` default.
  </action>
  <verify>
    <automated><![CDATA[cd server && ./mvnw -q -pl skillhub-app -DskipTests compile]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SkillCollectionReconciliationScheduler" server/skillhub-app/src/main/java/com/iflytek/skillhub/schedule/SkillCollectionReconciliationScheduler.java` exits 0.
    - `rg -n "reconcile-cron" server/skillhub-app/src/main/resources/application.yml` exits 0.
    - `rg -n "0 0 \\* \\* \\* \\*" server/skillhub-app/src/main/resources/application.yml` exits 0.
    - `./mvnw -q -pl skillhub-app -DskipTests compile` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: ADM-01 audit on admin-equivalent collection commands</name>
  <read_first>
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillLabelAppService.java` — `recordAudit` / `AuditLogService.record` usage.
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillCollectionPortalCommandAppService.java` — methods with `adminEquivalent` flag from plan 02-01.
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/portal/SkillCollectionController.java` — obtain `HttpServletRequest` for IP/UA like other controllers (`rg -n "AuditRequestContext" server/skillhub-app/src/main/java/com/iflytek/skillhub/controller` for pattern).
  </read_first>
  <action>
    1. Inject `AuditLogService` into `SkillCollectionPortalCommandAppService`.
    2. Add package-private helper `recordCollectionAudit(String actionCode, String actorUserId, Long collectionId, AuditRequestContext ctx, String jsonPayload)` calling `auditLogService.record` mirroring `SkillLabelAppService.recordAudit` parameter order discovered in source.
    3. For **every** public command method where `adminEquivalent == true` **and** the operation mutates data (`create` with target owner, `delete`, `setVisibility`, `updateMetadata` if admin allowed), emit audit with action codes:
       - `SKILL_COLLECTION_ADMIN_CREATE`
       - `SKILL_COLLECTION_ADMIN_UPDATE`
       - `SKILL_COLLECTION_ADMIN_VISIBILITY`
       - `SKILL_COLLECTION_ADMIN_DELETE`
    4. Update `SkillCollectionController` methods to construct `AuditRequestContext` from `HttpServletRequest` (client IP via `request.getRemoteAddr()`, user-agent via `request.getHeader("User-Agent")`) and pass into command service methods — **extend method signatures** as needed.

    Constants above must appear as string literals exactly in Java source for grep verification.
  </action>
  <verify>
    <automated><![CDATA[cd server && ./mvnw -q -pl skillhub-app -DskipTests compile]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SKILL_COLLECTION_ADMIN_DELETE" server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillCollectionPortalCommandAppService.java` exits 0.
    - `rg -n "AuditLogService" server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillCollectionPortalCommandAppService.java` exits 0.
    - `./mvnw -q -pl skillhub-app -DskipTests compile` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: [BLOCKING] make test-backend</name>
  <read_first>
    - `Makefile`
  </read_first>
  <action>
    Run `make test-backend` from repository root until exit 0.
  </action>
  <verify>
    <automated><![CDATA[make test-backend]]></automated>
  </verify>
  <acceptance_criteria>
    - `make test-backend` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Domain reconcile unit tests pass.
- Scheduler compiles; default cron property present.
- Admin audit literals grep-visible in command service.
- Full backend suite green.
</verification>

<success_criteria>
- INT-03 has an automated, observable reconciliation path.
- ADM-01 satisfied for admin-equivalent collection mutations via audit log records.
</success_criteria>
