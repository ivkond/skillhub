---
phase: 03-web-ui
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - server/skillhub-app/src/main/java/com/iflytek/skillhub/dto/collection/SkillCollectionResponse.java
  - server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillCollectionPortalQueryAppService.java
  - server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java
  - server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionSecurityIT.java
  - server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipServiceTest.java
autonomous: true
requirements:
  - WEB-03
  - ROL-06
  - VIS-03
must_haves:
  truths:
    - "`SkillCollectionResponse` exposes one aggregate field for contributor-only hidden-member count, null/zero for owner, admin, strangers, and all public API responses (D-09, no leak on public)."
    - "`SkillCollectionMembershipService` accepts contributor partial reorder lists and merges with full membership order without exposing hidden skill ids (D-11)."
    - "Automated tests prove contributor vs owner vs public JSON shapes for the new field and reorder merge."
  artifacts:
    - path: server/skillhub-app/src/main/java/com/iflytek/skillhub/dto/collection/SkillCollectionResponse.java
      provides: "Aggregate hidden-member signal for contributors"
      contains: "SkillCollectionResponse"
    - path: server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java
      provides: "Contributor-safe reorder merge"
      contains: "reorderSkills"
  key_links:
    - from: SkillCollectionPortalQueryAppService
      to: SkillCollectionResponse
      via: "DTO assembly for getForActor"
      pattern: "listVisibleMembers"
---

<objective>
Ship **server-side** support for Phase 3 UI: **D-09** (non-leaking aggregate count for contributors on authenticated collection reads) and **D-11** (contributor reorder without sending hidden skill ids), with regression tests. No web files in this plan.
</objective>

<threat_model>
**OWASP ASVS L1 — DTO and domain changes**

| Risk | Mitigation |
|------|------------|
| Information disclosure via new aggregate | Field is absent, null, or zero for public responses and strangers; never echo hidden skill ids or titles. |
| Authorization bypass on reorder | Domain service continues to enforce actor permissions; only ordering merge logic changes. |
| Test gaps | Extend `SkillCollectionSecurityIT` and `SkillCollectionMembershipServiceTest` for contributor + hidden-member scenarios. |
</threat_model>

<execution_context>
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@CLAUDE.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java
@server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillCollectionPortalQueryAppService.java
@server/skillhub-app/src/main/java/com/iflytek/skillhub/dto/collection/SkillCollectionResponse.java

<interfaces>
Authenticated reorder: extend `reorderSkills` so contributors may submit visible-subset order; load full membership server-side.

Public read remains unchanged; new aggregate must not be populated on public DTOs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-09 aggregate field + D-11 contributor-safe reorder + tests</name>
  <read_first>
    - `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java` — `reorderSkills` invariant.
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/service/SkillCollectionPortalQueryAppService.java` — `listVisibleMembers`, `getForActor`, `getPublicByOwnerAndSlug`.
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/dto/collection/SkillCollectionResponse.java`
    - `server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionSecurityIT.java`
    - `CLAUDE.md` — GitNexus impact before editing Java symbols.
  </read_first>
  <action>
    **D-09:** Extend `SkillCollectionResponse` with one aggregate (e.g. `additionalMembersHiddenFromActorCount`, JSON camelCase) where: value is null or 0 for owner, admin, stranger, and **all** `getPublicByOwnerAndSlug` / public controller paths; for `getForActor` when actor is contributor and hidden memberships exist, set count from server-side totals minus visible size without embedding hidden ids.

    Implement in `SkillCollectionPortalQueryAppService` when assembling the response.

    **D-11 / ROL-06:** Update `SkillCollectionMembershipService.reorderSkills` (or helper) so a contributor may submit `orderedSkillIds` for the visible subset only; service loads full membership and merges ordering (stable relative order for hidden ids) per **03-RESEARCH.md** resolved decision.

    Add/adjust `SkillCollectionSecurityIT` and `SkillCollectionMembershipServiceTest` for contributor + hidden skills, owner unchanged, public JSON unchanged.

    Run `cd server && ./mvnw -q -pl skillhub-app,skillhub-domain test`.
  </action>
  <verify>
    <automated><![CDATA[cd server && ./mvnw -q -pl skillhub-app,skillhub-domain test]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "additionalMembersHiddenFromActorCount|hiddenFromActor|HiddenFromActor" server/skillhub-app/src/main/java/com/iflytek/skillhub/dto/collection/SkillCollectionResponse.java` exits 0 (exact name as implemented).
    - `rg -n "reorderSkills" server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionMembershipService.java` exits 0.
    - `cd server && ./mvnw -q -pl skillhub-app,skillhub-domain test` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd server && ./mvnw -q -pl skillhub-app,skillhub-domain test` passes.
</verification>

<success_criteria>
- D-09 and D-11 behaviors are enforced in domain/query layer with automated proof.
- No new fields leak hidden membership data on public endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-02-SUMMARY.md`
</output>
