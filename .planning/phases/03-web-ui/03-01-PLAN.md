---
phase: 03-web-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/generated/schema.d.ts
  - web/src/api/client.ts
  - web/src/shared/hooks/use-collection-queries.ts
  - web/src/shared/hooks/use-collection-queries.test.ts
  - web/src/app/router.tsx
  - web/src/app/router.test.ts
  - web/src/pages/dashboard.tsx
  - web/src/shared/components/user-menu.tsx
  - web/src/pages/dashboard/collections-list.tsx
  - web/src/pages/dashboard/collection-new.tsx
  - web/src/pages/dashboard/collection-edit.tsx
  - web/src/i18n/locales/en.json
  - web/src/i18n/locales/zh.json
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - VIS-01
must_haves:
  truths:
    - "Authenticated user opens `/dashboard/collections` from a new dashboard card and from the user menu without typing URLs (D-01, D-02)."
    - "User creates a collection via `/dashboard/collections/new` and edits metadata/visibility via `/dashboard/collections/$collectionId/edit`, with server validation errors shown (WEB-02)."
    - "List page shows card grid with title, visibility, relationship (owner vs contributor), and last-updated when API provides it (D-04)."
    - "All new copy uses react-i18next keys in `en.json` and `zh.json`."
  artifacts:
    - path: web/src/app/router.tsx
      provides: "Registered routes `dashboard/collections`, `dashboard/collections/new`, `dashboard/collections/$collectionId/edit` with `beforeLoad: requireAuth`"
      contains: "collections"
    - path: web/src/api/client.ts
      provides: "Exported `collectionApi` calling `/api/web/me/collections`, POST/PATCH/DELETE/PATCH visibility under `/api/web/collections`"
      contains: "collectionApi"
    - path: web/src/shared/hooks/use-collection-queries.ts
      provides: "React Query hooks for list + create + update mutations with invalidation"
      contains: "collections"
  key_links:
    - from: web/src/pages/dashboard.tsx
      to: "/dashboard/collections"
      via: "<Link to=\"/dashboard/collections\">"
      pattern: "dashboard/collections"
    - from: web/src/shared/components/user-menu.tsx
      to: "/dashboard/collections"
      via: "<Link to=\"/dashboard/collections\">"
      pattern: "dashboard/collections"
    - from: web/src/shared/hooks/use-collection-queries.ts
      to: web/src/api/client.ts
      via: "collectionApi"
      pattern: "collectionApi\\."
---

<objective>
Ship **authenticated routing and pages** for “My collections”: dashboard + menu discovery (WEB-01), card-grid list (D-04), and create/edit collection forms with validation feedback (WEB-02), backed by typed API client and React Query hooks.

Purpose: Unblocks **03-02** detail/public/share work by fixing URLs, client contracts, and navigation entry points.

Output: New routes under `dashboard/collections/*`, `collectionApi` + hooks, refreshed OpenAPI types, dashboard card + user-menu link, minimal Vitest coverage for hooks.
</objective>

<threat_model>
**OWASP ASVS L1 — authenticated collections shell (WEB-01 / WEB-02)**

| Risk | Mitigation |
|------|------------|
| Broken access control on list/create/edit | Rely on `beforeLoad: requireAuth` for every `dashboard/collections/*` route; never call mutating endpoints without session; trust server 403/404 and map to safe UI (no extra probes of other users’ ids). |
| CSRF / session tampering on POST/PATCH/DELETE | Use existing `web/src/api/client.ts` patterns (`ensureCsrfHeaders` / `withRequestHeaders`) for all `collectionApi` mutations — no raw `fetch` bypassing CSRF. |
| Information disclosure (VIS-01, D-08 alignment on errors) | List and detail-preparation endpoints are authenticated only; render generic error states for failed loads without echoing raw stack traces or internal ids in user-facing copy. |
| Client-side trust of visibility | Treat `visibility` as display-only; do not infer permissions client-side beyond disabling obvious non-owner controls (full enforcement lands in **03-02**). |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@web/src/app/router.tsx
@web/src/pages/dashboard.tsx
@web/src/shared/hooks/use-namespace-queries.ts
@web/src/api/client.ts
@CLAUDE.md

<interfaces>
From `web/src/shared/hooks/use-namespace-queries.ts` (pattern to mirror):
- `useQuery` / `useMutation` with `queryKey` arrays and `invalidateQueries` on success.
- Thin `queryFn` wrappers delegating to `*Api` in `client.ts`.

From `web/src/app/router.tsx`:
- Lazy route components via `createLazyRouteComponent(() => import('...'), 'ExportName')`.
- Dashboard family: `path: 'dashboard/...'`, `beforeLoad: requireAuth`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate OpenAPI types and scaffold collectionApi</name>
  <read_first>
    - `web/package.json` — `generate-api` script (OpenAPI URL).
    - `web/src/api/client.ts` — `namespaceApi` / `WEB_API_PREFIX` / CSRF header helpers.
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/portal/SkillCollectionController.java` — exact paths (`/me/collections`, `/collections`, `/collections/{id}`, `/collections/{id}/visibility`).
    - `CLAUDE.md` — run `gitnexus_impact` before editing `collection*` symbols in `client.ts` (process requirement).
  </read_first>
  <action>
    1. **Wave 0 / OpenAPI:** With Spring Boot running on `localhost:8080`, run `cd web && pnpm run generate-api` to refresh `web/src/api/generated/schema.d.ts`. If the executor cannot run the backend in CI/sandbox, document in the phase SUMMARY the exact commit hash of `schema.d.ts` produced on a machine where generation succeeded, and still land `collectionApi` using the controller paths above (typed against `paths` once generation works).
    2. In `web/src/api/client.ts`, add **`collectionApi`** with typed functions (names fixed): `listMine({ page, size })` → `GET /api/web/me/collections`; `create(body)` → `POST /api/web/collections`; `getById(id)` → `GET /api/web/collections/{id}`; `updateMetadata(id, body)` → `PATCH /api/web/collections/{id}`; `setVisibility(id, visibility)` → `PATCH /api/web/collections/{id}/visibility`; `deleteCollection(id)` → `DELETE /api/web/collections/{id}`. Reuse the same envelope + error mapping as `namespaceApi` (per D-03 use `/api/web` only).
    3. Export `collectionApi` next to other API groups.
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm run typecheck]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "collectionApi" web/src/api/client.ts` exits 0.
    - `rg -n "/me/collections" web/src/api/client.ts` exits 0.
    - `rg -n "/api/web/me/collections|/api/web/collections" web/src/api/generated/schema.d.ts` exits 0 **or** SUMMARY documents blocked OpenAPI run with controller-aligned manual typings committed.
    - `cd web && pnpm run typecheck` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: use-collection-queries hooks + unit tests</name>
  <read_first>
    - `web/src/shared/hooks/use-namespace-queries.ts` — invalidation helpers.
    - `web/src/shared/hooks/use-namespace-queries.test.ts` — Vitest patterns (if present; else `web/src/pages/dashboard.test.tsx` or nearest hook test).
    - `web/src/api/client.ts` — `collectionApi` from Task 1.
  </read_first>
  <action>
    Create **`web/src/shared/hooks/use-collection-queries.ts`** exporting at minimum:
    - `useMyCollections(params)` — `queryKey: ['collections','mine', page, size]`, `queryFn` → `collectionApi.listMine`.
    - `useCollectionDetail(id)` — `queryKey: ['collections', id]`, `enabled: !!id`.
    - `useCreateCollection()` / `useUpdateCollection()` / `useSetCollectionVisibility()` / `useDeleteCollection()` mutations that invalidate `['collections','mine']` and `['collections', id]` on success.

    Add **`web/src/shared/hooks/use-collection-queries.test.ts`** with mocked `collectionApi` verifying query keys and that `useCreateCollection` invalidates list queries on success (mirror existing hook test style).
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm exec vitest run src/shared/hooks/use-collection-queries.test.ts]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "useMyCollections" web/src/shared/hooks/use-collection-queries.ts` exits 0.
    - `rg -n "describe\\(" web/src/shared/hooks/use-collection-queries.test.ts` exits 0.
    - `cd web && pnpm exec vitest run src/shared/hooks/use-collection-queries.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Router + list/create/edit pages + dashboard + menu + i18n</name>
  <read_first>
    - `web/src/pages/dashboard/namespace-members.tsx` — page shell / `APP_SHELL_PAGE_CLASS_NAME` usage.
    - `web/src/app/router.tsx` — lazy route registration pattern.
    - `web/src/pages/dashboard.tsx` — card grid for D-01.
    - `web/src/shared/components/user-menu.tsx` — dropdown link placement for D-02.
    - `web/src/i18n/locales/en.json` — key naming for `dashboard.*`.
  </read_first>
  <action>
    **Routing (D-03 default):** Register TanStack routes (all `beforeLoad: requireAuth`):
    - `path: 'dashboard/collections'` → export `CollectionsListPage` from `web/src/pages/dashboard/collections-list.tsx`.
    - `path: 'dashboard/collections/new'` → `CollectionNewPage` from `web/src/pages/dashboard/collection-new.tsx`.
    - `path: 'dashboard/collections/$collectionId/edit'` → `CollectionEditPage` from `web/src/pages/dashboard/collection-edit.tsx` (use string `collectionId` param matching backend `Long` id as string).

    **List page (D-04, WEB-01):** Card grid using existing `Card` components; each card links to `/dashboard/collections/$collectionId/edit` or a neutral “manage” path that still lives under collections; show visibility badge, role text (`owner` vs `contributor`) from API fields (`ownerId` vs current user), `updatedAt` if returned else omit quietly.

    **Create page (WEB-02):** Form fields: `title`, `description`, `visibility` select (`PUBLIC`/`PRIVATE`), `slug` per `SkillCollectionCreateRequest` server validation; on success navigate to **`/dashboard/collections/$collectionId/edit`** until plan **03-03** adds the detail route (then switch default navigation to **`/dashboard/collections/$collectionId`**).

    **Edit page (WEB-02):** Load `useCollectionDetail(collectionId)`; PATCH metadata + PATCH visibility in separate actions or one save — match existing portal patterns; handle `ApiError` field messages in-place.

    **Dashboard card (D-01):** Add a fifth card in the quick-link grid linking to `/dashboard/collections` with new i18n keys `dashboard.collectionsTitle` / `dashboard.openCollections`.

    **User menu (D-02):** Add `<Link to="/dashboard/collections">` with i18n label in the same menu block as other dashboard shortcuts (discoverable text link).

    **i18n:** Add matching keys under `collections` namespace (e.g. `collections.title`, `collections.create`, validation labels) in **`web/src/i18n/locales/en.json`** and **`web/src/i18n/locales/zh.json`**.

    **Router test:** Extend `web/src/app/router.test.ts` to assert `router.routeTree` contains path segments for `dashboard/collections` (string match on route id or path config as done elsewhere).

    **Process:** Before editing `router.tsx` exports, run **`gitnexus_impact`** on the lazy route factory or new page symbols per `CLAUDE.md`; run **`gitnexus_detect_changes`** before commit.
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm exec vitest run src/app/router.test.ts src/shared/hooks/use-collection-queries.test.ts && pnpm run typecheck]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "dashboard/collections" web/src/app/router.tsx` exits 0 with at least three route definitions.
    - `rg -n "to=\"/dashboard/collections\"" web/src/pages/dashboard.tsx web/src/shared/components/user-menu.tsx` exits 0.
    - `rg -n "collections\\." web/src/i18n/locales/en.json` exits 0.
    - `cd web && pnpm exec vitest run src/app/router.test.ts src/shared/hooks/use-collection-queries.test.ts` exits 0.
    - `cd web && pnpm run typecheck` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd web && pnpm exec vitest run src/app/router.test.ts src/shared/hooks/use-collection-queries.test.ts && pnpm run typecheck` passes.
- Manual smoke: sign in, open `/dashboard/collections`, create collection, land on edit (or detail after 03-03), list shows new card.
</verification>

<success_criteria>
- WEB-01 and WEB-02 behaviors are reachable entirely through authenticated UI routes.
- VIS-01 respected implicitly by server filtering; UI does not attempt to list other users’ private collections via crafted ids beyond normal navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-01-SUMMARY.md`
</output>
