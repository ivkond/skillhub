---
phase: 03-web-ui
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - web/src/app/router.tsx
  - web/src/app/router.test.ts
  - web/src/pages/dashboard/collection-new.tsx
  - web/src/pages/dashboard/collection-detail.tsx
  - web/src/pages/public/public-collection-page.tsx
  - web/src/shared/hooks/use-collection-queries.ts
  - web/src/shared/hooks/use-skill-by-id.ts
  - web/src/api/client.ts
  - web/src/features/collection/add-collection-contributor-dialog.tsx
  - web/src/features/collection/collection-share-actions.tsx
  - web/src/features/collection/collection-skill-rows.tsx
  - web/src/i18n/locales/en.json
  - web/src/i18n/locales/zh.json
autonomous: true
requirements:
  - WEB-03
  - WEB-04
  - WEB-05
  - VIS-02
  - VIS-03
  - ROL-02
  - ROL-03
must_haves:
  truths:
    - "Owner and contributor use `/dashboard/collections/$collectionId` for ordered skills, links to `/space/$namespace/$slug`, up/down reorder (D-12), empty CTA (D-13), D-09/D-10/D-15 behaviors using aggregate from plan 02."
    - "Owner manages contributors on the same page (D-16–D-19, WEB-05)."
    - "Public `/u/$ownerKey/c/$collectionSlug` uses public GET then authenticated `GET /api/web/collections/{id}` when session exists (VIS-03); generic Not Found for D-08."
  artifacts:
    - path: web/src/pages/dashboard/collection-detail.tsx
      provides: "Authenticated detail + reorder + contributors + share"
      contains: "collectionId"
    - path: web/src/pages/public/public-collection-page.tsx
      provides: "Public shell + dual-fetch when logged in"
      contains: "ownerKey"
  key_links:
    - from: web/src/pages/public/public-collection-page.tsx
      to: GET /api/web/public/collections/{ownerId}/{slug}
      via: "collectionApi public read"
      pattern: "public/collections"
    - from: web/src/pages/public/public-collection-page.tsx
      to: GET /api/web/collections/{id}
      via: "session refetch"
      pattern: "collections/"
---

<objective>
Implement **collection detail**, **contributor management**, **share affordances**, and **public collection page** in the React app, consuming plan **02** backend contracts. Closes RESEARCH integration gaps for logged-in public viewers and D-09/D-11 UX.
</objective>

<threat_model>
**OWASP ASVS L1 — detail, contributors, share, public**

| Risk | Mitigation |
|------|------------|
| Broken access control | Server authoritative; hide owner-only UI unless `currentUser` is owner; public route GET-only until authenticated refetch. |
| CSRF | All mutations via `client.ts` with CSRF headers. |
| Information disclosure (D-08, VIS-03) | Generic Not Found for forbidden/missing; render only API-filtered members; skill enrichment only for visible member ids. |
| D-09 leak on public | Do not display aggregate hidden count on public page (field is null from plan 02); helper text only on authenticated contributor views. |
</threat_model>

<execution_context>
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-UI-SPEC.md
@CLAUDE.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@web/src/pages/dashboard/namespace-members.tsx
@web/src/features/namespace/add-namespace-member-dialog.tsx
@server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/portal/PublicSkillCollectionController.java

<interfaces>
Reorder mutation must send the contract produced by plan **02** (partial list for contributors where supported).

Skill rows: batch `GET /api/web/skills/id/{skillId}` only for visible member ids.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail route — skills, reorder, D-09 UI, empty state, skill enrichment</name>
  <read_first>
    - `.planning/phases/03-web-ui/03-01-PLAN.md` — confirmed `collectionApi` / hook surface from plan 01.
    - `web/src/pages/dashboard/namespace-members.tsx` — `ConfirmDialog` pattern.
    - `web/src/pages/dashboard.tsx` — `Link` to `/space/$namespace/$slug`.
    - `web/src/api/client.ts` — extend for skill-by-id if missing.
  </read_first>
  <action>
    1. Register `path: 'dashboard/collections/$collectionId'` in `web/src/app/router.tsx` with `beforeLoad: requireAuth`, lazy **`CollectionDetailPage`** from `web/src/pages/dashboard/collection-detail.tsx`.

    2. Extend `use-collection-queries.ts` with reorder/add/remove skill mutations calling existing REST paths via `collectionApi`.

    3. Implement `web/src/features/collection/collection-skill-rows.tsx` (or colocate): sort by `sortOrder`, resolve title + namespace via `web/src/shared/hooks/use-skill-by-id.ts` + `GET /api/web/skills/id/{skillId}`; primary link `<Link to="/space/$namespace/$slug" ...>`.

    4. Reorder: up/down per row; call mutation per plan **02** contract.

    5. **D-09 / D-10 / D-15:** If aggregate from API &gt; 0, show i18n helper without listing hidden skills; owner: no extra banner; disable owner-only actions for contributors.

    6. **Empty (D-13):** CTA to add skill when list empty.

    7. Update **03-01** create success navigation to `/dashboard/collections/$collectionId` now that detail exists.

    If plan **02** aggregate is missing in JSON, disable contributor reorder when policy requires it; document in SUMMARY (temporary).
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm exec vitest run src/shared/hooks/use-collection-queries.test.ts && pnpm run typecheck]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "dashboard/collections/\\$collectionId|collections/\\$collectionId" web/src/app/router.tsx` exits 0.
    - `rg -n "collection-detail|CollectionDetailPage" web/src/pages/dashboard/collection-detail.tsx web/src/app/router.tsx` exits 0.
    - `rg -n "skills/id" web/src/api/client.ts` exits 0.
    - `cd web && pnpm run typecheck` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Contributors, share, public page + authenticated refetch</name>
  <read_first>
    - `web/src/features/namespace/add-namespace-member-dialog.tsx`
    - `web/src/pages/dashboard/namespace-members.tsx`
    - `web/src/api/client.ts`
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/portal/PublicSkillCollectionController.java`
  </read_first>
  <action>
    **Contributors:** Owner-only section on detail page; `add-collection-contributor-dialog.tsx` mirroring namespace member search; remove with `ConfirmDialog`.

    **Share:** `collection-share-actions.tsx` — public: copy `${origin}/u/${ownerKey}/c/${slug}`; private: copy allowed with signed-in notice (D-07).

    **Public route:** `path: 'u/$ownerKey/c/$collectionSlug'` without `requireAuth`, `web/src/pages/public/public-collection-page.tsx`.

    **Logged-in on public URL:** `GET` public then if session exists and `id` present, `GET /api/web/collections/{id}` and replace payload; 403/404 → generic Not Found (D-08).

    **i18n:** New keys in `en.json` / `zh.json`.

    **Tests:** Extend `web/src/app/router.test.ts` for `u/` route.

    GitNexus per `CLAUDE.md` before commits.
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm exec vitest run src/app/router.test.ts && pnpm run typecheck]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ownerKey" web/src/app/router.tsx` exits 0 and `rg -n "collectionSlug" web/src/app/router.tsx` exits 0.
    - `rg -n "getPublic|public/collections" web/src/api/client.ts web/src/pages/public/public-collection-page.tsx` exits 0.
    - `rg -n "getCurrentUser|useSession|auth" web/src/pages/public/public-collection-page.tsx` exits 0 (session gate for refetch — match actual auth hook name in repo).
    - `rg -n "contributors" web/src/api/client.ts` exits 0.
    - `cd web && pnpm exec vitest run src/app/router.test.ts` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd web && pnpm exec vitest run src/app/router.test.ts src/shared/hooks/use-collection-queries.test.ts && pnpm run typecheck` passes.
- Manual: anonymous public URL shows filtered list; signed-in refetch improves visibility; private stranger sees generic Not Found.
</verification>

<success_criteria>
- WEB-03..WEB-05 and VIS-02/03 satisfied without violating D-07/D-08.
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-03-SUMMARY.md`
</output>
