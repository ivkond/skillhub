---
phase: 04-verification-docs
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
reviews_applied: "2026-04-16"
files_modified:
  - document/docs/04-developer/api/authenticated.md
  - document/i18n/en/docusaurus-plugin-content-docs/current/04-developer/api/authenticated.md
  - docs/e2e.md
autonomous: true
requirements:
  - QA-03
must_haves:
  truths:
    - "Developer-facing documentation explains collection endpoints, roles, visibility-safe behavior, and verification usage."
    - "Documentation is linked into existing doc structure rather than standalone orphan content."
    - "E2E scenario references are clear enough for local and CI verification."
    - "Docusaurus `npm run build` in `document/` succeeds after doc edits (link integrity)."
    - "Collections subsection uses the same `##` / `###` heading skeleton in primary and English mirror files."
  artifacts:
    - path: document/docs/04-developer/api/authenticated.md
      provides: "Collections API usage and endpoint behavior notes."
      contains: "collections"
    - path: docs/e2e.md
      provides: "E2E scenario and run instructions including collections specs."
      contains: "collections-flow.spec.ts"
---

<objective>
Ship `QA-03` by updating developer-facing docs with collection endpoint details and verification guidance, linked to existing documentation navigation. Apply review feedback: **checklist-style API notes**, **docs build**, **i18n heading parity**, and **traceability** to tests from 04-01/04-02 SUMMARYs.
</objective>

<context>
@.planning/phases/04-verification-docs/04-CONTEXT.md
@.planning/phases/04-verification-docs/04-REVIEWS.md
@.planning/phases/04-verification-docs/04-VALIDATION.md
@.planning/phases/04-verification-docs/04-01-SUMMARY.md
@.planning/phases/04-verification-docs/04-02-SUMMARY.md
@document/docs/04-developer/api/authenticated.md
@document/i18n/en/docusaurus-plugin-content-docs/current/04-developer/api/authenticated.md
@docs/e2e.md
</context>

<threat_model>
Docs must not instruct clients to bypass server visibility checks. Examples use anonymized or clearly fictional slugs; do not embed production secrets or real tokens.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Document collections authenticated API surface</name>
  <read_first>
    - `document/docs/04-developer/api/authenticated.md`
    - `.planning/phases/04-verification-docs/04-01-SUMMARY.md`
    - `.planning/phases/04-verification-docs/04-02-SUMMARY.md`
  </read_first>
  <action>
    Update authenticated developer API docs with a concise section covering: collection routes (method + path pattern), **owner vs contributor vs stranger vs admin** semantics, visibility-safe read behavior, and common error statuses. Add a short **traceability** line referencing `04-01-SUMMARY.md` / `04-02-SUMMARY.md` test names where helpful.
    **OpenAPI:** If the repo publishes generated OpenAPI, add one sentence pointing to where it lives and that collections routes must appear after publish — do not duplicate the full OpenAPI in markdown unless the team standard requires it.
  </action>
  <verify>
    <automated><![CDATA[rg -n "collections" document/docs/04-developer/api/authenticated.md document/i18n/en/docusaurus-plugin-content-docs/current/04-developer/api/authenticated.md]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "collections" document/docs/04-developer/api/authenticated.md` exits 0.
    - `rg -n "collections" document/i18n/en/docusaurus-plugin-content-docs/current/04-developer/api/authenticated.md` exits 0.
  </acceptance_criteria>
  <done>Authenticated API docs updated (both locales).</done>
</task>

<task type="auto">
  <name>Task 2: Document E2E scenarios and execution commands</name>
  <read_first>
    - `docs/e2e.md`
    - `.github/workflows/pr-e2e.yml`
  </read_first>
  <action>
    Update `docs/e2e.md` with the phase 4 collections specs, **exact** `pnpm`/`playwright` commands, and how CI runs them (reference `pr-e2e.yml` job name if stable).
  </action>
  <verify>
    <automated><![CDATA[rg -n "collections-flow\.spec\.ts|collections-visibility-guard\.spec\.ts" docs/e2e.md]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "collections-flow\\.spec\\.ts" docs/e2e.md` exits 0.
    - `rg -n "collections-visibility-guard\\.spec\\.ts" docs/e2e.md` exits 0.
  </acceptance_criteria>
  <done>docs/e2e.md lists both collection specs.</done>
</task>

<task type="auto">
  <name>Task 3: i18n mirror heading parity for collections block</name>
  <read_first>
    - `document/docs/04-developer/api/authenticated.md`
    - `document/i18n/en/docusaurus-plugin-content-docs/current/04-developer/api/authenticated.md`
  </read_first>
  <action>
    For the **collections** documentation block only: ensure `document/docs/04-developer/api/authenticated.md` and `document/i18n/en/docusaurus-plugin-content-docs/current/04-developer/api/authenticated.md` use the same markdown heading levels and order (`##` / `###`) for that section so translators can diff structure.
  </action>
  <verify>
    <automated><![CDATA[rg -n "^#{1,3} " document/docs/04-developer/api/authenticated.md document/i18n/en/docusaurus-plugin-content-docs/current/04-developer/api/authenticated.md]]></automated>
  </verify>
  <acceptance_criteria>
    - Both files contain a `##` or `###` heading line matching the collections section skeleton (manual spot-check if automated diff is insufficient).
  </acceptance_criteria>
  <done>Heading parity reviewed for collections block.</done>
</task>

<task type="auto">
  <name>Task 4: Docusaurus build (blocking for this wave)</name>
  <read_first>
    - `document/package.json` — scripts
  </read_first>
  <action>
    From repo root: `cd document && npm run build` (use `pnpm` instead only if `document/pnpm-lock.yaml` exists and CI uses pnpm). Fix any broken links or MDX errors reported by the build.
  </action>
  <verify>
    <automated><![CDATA[cd document && npm run build]]></automated>
  </verify>
  <acceptance_criteria>
    - `cd document && npm run build` exits 0.
  </acceptance_criteria>
  <done>Docusaurus build green.</done>
</task>

<task type="auto">
  <name>Task 5: Write 04-03-SUMMARY</name>
  <read_first>
    - `.planning/phases/04-verification-docs/04-VALIDATION.md`
  </read_first>
  <action>
    Create `04-03-SUMMARY.md` with: docs build outcome, OpenAPI pointer sentence (if added), and links to edited files.
  </action>
  <verify>
    <automated><![CDATA[rg -n "build|OpenAPI|authenticated|e2e" .planning/phases/04-verification-docs/04-03-SUMMARY.md]]></automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-verification-docs/04-03-SUMMARY.md` exists.
    - `rg -n "npm run build|document" .planning/phases/04-verification-docs/04-03-SUMMARY.md` exits 0.
  </acceptance_criteria>
  <done>04-03-SUMMARY written.</done>
</task>
</tasks>

<verification>
- Docs include collections API guidance and are discoverable in existing docs tree.
- E2E documentation references the exact phase 4 specs and execution path.
- `cd document && npm run build` completes with exit code 0.
</verification>

<success_criteria>
- QA-03 satisfied with merged, linked, and actionable documentation updates.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-docs/04-03-SUMMARY.md` noting docs build result and any OpenAPI pointer added.
</output>
