---
phase: 04-verification-docs
plan: "01"
type: execute
wave: 1
depends_on: []
reviews_applied: "2026-04-16"
files_modified:
  - server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionSecurityIT.java
  - server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionControllerIT.java
  - server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainServiceTest.java
autonomous: true
requirements:
  - QA-01
must_haves:
  truths:
    - "Authorization tests cover owner/contributor/stranger/admin for representative high-risk collection routes."
    - "Matrix includes both mutation and sensitive read behavior to prevent role regressions."
    - "INT-03 has at least one named regression test (integration or domain) with explicit visible→unreadable transition assertions."
    - "04-01-SUMMARY.md contains a route × role × expected outcome table (or equivalent) copied from implemented tests."
  artifacts:
    - path: server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionSecurityIT.java
      provides: "Role matrix assertions for collection HTTP endpoints."
      contains: "owner"
    - path: server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainServiceTest.java
      provides: "Domain-level guard coverage for permissions, membership invariants, and INT-03 regression."
      contains: "contributor"
---

<objective>
Complete backend verification for `QA-01` by extending integration and domain tests around the highest-risk permission and visibility paths for collections, and close review gaps: explicit **auth matrix evidence** in SUMMARY and **INT-03** regression coverage (Phase 2 already implements behavior; Phase 4 proves it).

Scope emphasis: representative confidence, not exhaustive endpoint permutations.
</objective>

<context>
@.planning/phases/04-verification-docs/04-CONTEXT.md
@.planning/phases/04-verification-docs/04-REVIEWS.md
@.planning/phases/04-verification-docs/04-VALIDATION.md
@.planning/REQUIREMENTS.md
@server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionSecurityIT.java
@server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionControllerIT.java
@server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainServiceTest.java
</context>

<threat_model>
Phase 4 verification only. Tests must assert **no authorization bypass** and **no cross-tenant or cross-user data leakage** on collection routes under test. Admin assertions must respect `RouteSecurityPolicyRegistry` (admin is not superuser outside policy).
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Expand security integration matrix for key collection routes</name>
  <read_first>
    - `server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionSecurityIT.java`
    - `server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/portal/SkillCollectionController.java` — route prefixes
    - `.planning/phases/04-verification-docs/04-CONTEXT.md` — D-01, D-02
  </read_first>
  <action>
    Add matrix cases for owner/contributor/stranger/admin on representative routes:
    - collection metadata updates
    - contributor management commands
    - membership mutation commands
    - filtered public/private read edges
    For each new case, assert **HTTP status** and, where response bodies could leak titles/slugs/metadata, assert **absence or shape** of forbidden fields (not status alone).
  </action>
  <verify>
    <automated><![CDATA[./gradlew :server:skillhub-app:test --tests "*SkillCollectionSecurityIT*"]]></automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :server:skillhub-app:test --tests "*SkillCollectionSecurityIT*"` exits 0.
    - `rg -n "owner|contributor|stranger|admin" server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionSecurityIT.java` exits 0.
  </acceptance_criteria>
  <done>SecurityIT matrix extended; Gradle SecurityIT slice green.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten domain service tests for role-bound behavior</name>
  <read_first>
    - `server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainServiceTest.java`
    - `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainService.java`
  </read_first>
  <action>
    Add or refine unit tests in `SkillCollectionDomainServiceTest` validating contributor boundaries, admin override behavior, and duplicate/ordering invariants under role-specific operations.
  </action>
  <verify>
    <automated><![CDATA[./gradlew :server:skillhub-domain:test --tests "*SkillCollectionDomainServiceTest*"]]></automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :server:skillhub-domain:test --tests "*SkillCollectionDomainServiceTest*"` exits 0.
    - `rg -n "contributor" server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection/SkillCollectionDomainServiceTest.java` exits 0.
  </acceptance_criteria>
  <done>Domain tests updated; Gradle domain slice green.</done>
</task>

<task type="auto">
  <name>Task 3: INT-03 named regression (domain preferred, integration if needed)</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — INT-03 wording
    - `SkillCollectionDomainServiceTest.java` and domain service implementation for reconciliation hooks
  </read_first>
  <action>
    Add a **named** test method (name contains `Int03`, `Lifecycle`, or `UnreadableSkill`) that covers INT-03: when a skill becomes unreadable to the acting user (or globally per existing domain API), **membership or read path** matches the reconciled product behavior. Prefer `SkillCollectionDomainServiceTest`; add to `SkillCollectionSecurityIT` only if HTTP surface must be asserted.
  </action>
  <verify>
    <automated><![CDATA[./gradlew :server:skillhub-domain:test --tests "*SkillCollectionDomainServiceTest*" && ./gradlew :server:skillhub-app:test --tests "*SkillCollectionSecurityIT*"]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Int03|Lifecycle|UnreadableSkill|INT-03|int03" server/skillhub-domain/src/test/java/com/iflytek/skillhub/domain/collection server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal` exits 0.
  </acceptance_criteria>
  <done>INT-03 regression test present and named; Gradle green.</done>
</task>

<task type="auto">
  <name>Task 4: Controller IT — reference or extend</name>
  <read_first>
    - `server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/portal/SkillCollectionControllerIT.java`
    - `SkillCollectionSecurityIT.java`
  </read_first>
  <action>
    Per **D-13:** read `SkillCollectionControllerIT.java`. If all new coverage lives in SecurityIT + domain tests, add a one-line comment in Controller IT pointing to SecurityIT as canonical matrix **or** add missing happy-path assertions only if a gap exists. Do not duplicate the full matrix in Controller IT.
  </action>
  <verify>
    <automated><![CDATA[./gradlew :server:skillhub-app:test --tests "*SkillCollectionControllerIT*" --tests "*SkillCollectionSecurityIT*"]]></automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :server:skillhub-app:test --tests "*SkillCollectionControllerIT*"` exits 0.
  </acceptance_criteria>
  <done>Controller IT aligned with D-13; Gradle green.</done>
</task>

<task type="auto">
  <name>Task 5: Write 04-01-SUMMARY with route × role matrix</name>
  <read_first>
    - `.planning/phases/04-verification-docs/04-CONTEXT.md` — D-10
    - `SkillCollectionSecurityIT.java` — final test method names
  </read_first>
  <action>
    Create `04-01-SUMMARY.md` with a markdown **table**: route (method + path pattern), role, expected status/outcome, test method reference; include a line pointing to the INT-03 test method name(s).
  </action>
  <verify>
    <automated><![CDATA[rg -n "\\|.*\\|.*\\|" .planning/phases/04-verification-docs/04-01-SUMMARY.md && rg -n "INT-03|Int03|Lifecycle|UnreadableSkill" .planning/phases/04-verification-docs/04-01-SUMMARY.md]]></automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-verification-docs/04-01-SUMMARY.md` exists.
    - `rg -n "\\|"` on that file finds at least 3 pipe-separated table rows (header + 2 data rows minimum).
  </acceptance_criteria>
  <done>SUMMARY written with matrix and INT-03 pointer.</done>
</task>
</tasks>

<verification>
- `./gradlew :server:skillhub-app:test --tests "*SkillCollectionSecurityIT*"` passes.
- `./gradlew :server:skillhub-domain:test --tests "*SkillCollectionDomainServiceTest*"` passes.
- `./gradlew :server:skillhub-app:test --tests "*SkillCollectionControllerIT*"` passes.
- `04-01-SUMMARY.md` exists and contains a route × role table and INT-03 reference.
</verification>

<success_criteria>
- QA-01 satisfied with representative role matrix confidence on high-risk routes.
- INT-03 regression named and traceable from SUMMARY.
</success_criteria>

<output>
After completion, ensure `.planning/phases/04-verification-docs/04-01-SUMMARY.md` is committed with the phase work (Task 5 produces it).
</output>
