---
phase: 04-verification-docs
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
reviews_applied: "2026-04-16"
files_modified:
  - web/e2e/collections-flow.spec.ts
  - web/e2e/collections-visibility-guard.spec.ts
  - web/e2e/helpers/auth-fixtures.ts
  - .github/workflows/pr-e2e.yml
autonomous: true
requirements:
  - QA-02
must_haves:
  truths:
    - "One end-to-end happy path covers create collection, add skill, share/open public page, and contributor add/remove."
    - "At least one guard case proves restricted or non-visible data is not leaked (explicit actor + resource + expectation documented in SUMMARY)."
    - "Scenarios run locally and in CI with existing Playwright infrastructure."
    - "04-02-SUMMARY.md lists fixture users/skills, guard definition, anti-flake notes, and whether INT-03 is covered in E2E or deferred with rationale."
  artifacts:
    - path: web/e2e/collections-flow.spec.ts
      provides: "Primary collections happy path."
      contains: "create collection"
    - path: web/e2e/collections-visibility-guard.spec.ts
      provides: "Permission/visibility non-leak guard scenario."
      contains: "forbidden"
---

<objective>
Implement `QA-02` with stable Playwright coverage: one core collections journey plus one explicit permission/visibility guard scenario, integrated into existing local and CI E2E execution. Incorporate review feedback: **fixture contract**, **assertions on hidden/forbidden content**, **anti-flake**, **mandatory PR specs list**, and **INT-03** stance documented or covered.
</objective>

<context>
@.planning/phases/04-verification-docs/04-CONTEXT.md
@.planning/phases/04-verification-docs/04-REVIEWS.md
@.planning/phases/04-verification-docs/04-VALIDATION.md
@web/playwright.config.ts
@web/e2e/public-pages.spec.ts
@web/e2e/dashboard-routes.spec.ts
@.github/workflows/pr-e2e.yml
</context>

<threat_model>
E2E must not assert on stable internal IDs in URLs shown to users unless already product contract. Prefer **behavioral** assertions: correct HTTP/route from browser, absence of private skill titles in DOM/network for forbidden viewers.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add primary collections happy-path E2E</name>
  <read_first>
    - `web/e2e/helpers/auth-fixtures.ts`
    - `web/e2e/public-pages.spec.ts`
    - `web/playwright.config.ts`
  </read_first>
  <action>
    Create `collections-flow.spec.ts` to cover:
    1) sign-in and navigate to collections
    2) create collection
    3) add skill (use **documented** fixture skill from seed — same slug/title as in SUMMARY)
    4) share/open public URL behavior — assert **public** expectations (filtered members) with **wait** for navigation/network idle per existing `public-pages.spec.ts` patterns
    5) owner adds/removes contributor
    After each critical step, assert at least one **DOM or API response** condition that would fail if visibility were wrong (not only 200 OK).
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm exec playwright test e2e/collections-flow.spec.ts --config=playwright.config.ts]]></automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec playwright test e2e/collections-flow.spec.ts --config=playwright.config.ts` exits 0.
    - `rg -n "create collection|collections-flow" web/e2e/collections-flow.spec.ts` exits 0.
  </acceptance_criteria>
  <done>Happy-path spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Add visibility/permission guard E2E</name>
  <read_first>
    - `web/e2e/dashboard-routes.spec.ts`
    - `collections-flow.spec.ts` (after Task 1)
  </read_first>
  <action>
    Create `collections-visibility-guard.spec.ts` with **one primary guard** agreed in SUMMARY, e.g. stranger (or unauthenticated where applicable) cannot load **private** collection detail **or** must not see **private skill** fields on a public collection page — pick one concrete scenario and stick to it. Assert **forbidden route**, **masked UI**, or **empty member list** with explicit selectors/strings from implementation.
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm exec playwright test e2e/collections-visibility-guard.spec.ts --config=playwright.config.ts]]></automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec playwright test e2e/collections-visibility-guard.spec.ts --config=playwright.config.ts` exits 0.
    - `rg -n "forbidden|403|not.*visible|private" web/e2e/collections-visibility-guard.spec.ts` exits 0.
  </acceptance_criteria>
  <done>Guard spec green.</done>
</task>

<task type="auto">
  <name>Task 3: INT-03 in E2E (optional — document if skipped)</name>
  <read_first>
    - `.planning/phases/04-verification-docs/04-01-SUMMARY.md` — INT-03 test pointer
  </read_first>
  <action>
    If the environment can deterministically make a collection member skill **unreadable**, add one assertion that the member row disappears or UI matches Phase 3 behavior. If not feasible in Playwright within this milestone, write in `04-02-SUMMARY.md` under **INT-03**: “E2E not used; covered by &lt;class#method&gt; from 04-01” with accurate reference.
  </action>
  <verify>
    <automated><![CDATA[cd web && pnpm exec playwright test e2e/collections-flow.spec.ts e2e/collections-visibility-guard.spec.ts --config=playwright.config.ts]]></automated>
  </verify>
  <acceptance_criteria>
    - Combined Playwright command exits 0.
  </acceptance_criteria>
  <done>INT-03 E2E or deferral documented in SUMMARY draft path (finalized in Task 5).</done>
</task>

<task type="auto">
  <name>Task 4: CI workflow lists both specs explicitly</name>
  <read_first>
    - `.github/workflows/pr-e2e.yml`
  </read_first>
  <action>
    Update `.github/workflows/pr-e2e.yml` so the job that runs Playwright **explicitly** includes `e2e/collections-flow.spec.ts` and `e2e/collections-visibility-guard.spec.ts` (by path or project), not only a glob that might silently omit new files.
  </action>
  <verify>
    <automated><![CDATA[rg -n "collections-flow\.spec|collections-visibility-guard\.spec" .github/workflows/pr-e2e.yml]]></automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "collections-flow\\.spec" .github/workflows/pr-e2e.yml` exits 0.
    - `rg -n "collections-visibility-guard\\.spec" .github/workflows/pr-e2e.yml` exits 0.
  </acceptance_criteria>
  <done>CI workflow references both spec paths.</done>
</task>

<task type="auto">
  <name>Task 5: Write 04-02-SUMMARY (fixtures, guard, INT-03 stance)</name>
  <read_first>
    - `.planning/phases/04-verification-docs/04-CONTEXT.md` — D-11
    - `web/e2e/collections-flow.spec.ts`
    - `web/e2e/collections-visibility-guard.spec.ts`
  </read_first>
  <action>
    Create `04-02-SUMMARY.md` with: fixture users/skills table; one-sentence **guard definition**; anti-flake notes; explicit **INT-03** subsection (E2E coverage or deferral with pointer to 04-01 test names).
  </action>
  <verify>
    <automated><![CDATA[rg -n "fixture|guard|INT-03|anti-flake|flaky" .planning/phases/04-verification-docs/04-02-SUMMARY.md]]></automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-verification-docs/04-02-SUMMARY.md` exists.
    - `rg -n "INT-03|Int03|defer|E2E" .planning/phases/04-verification-docs/04-02-SUMMARY.md` exits 0.
    - `rg -n "guard|fixture" .planning/phases/04-verification-docs/04-02-SUMMARY.md` exits 0.
  </acceptance_criteria>
  <done>04-02-SUMMARY complete.</done>
</task>
</tasks>

<verification>
- New collections E2E specs pass locally with existing Playwright config.
- CI E2E workflow includes and executes both specs (verified by `rg` on workflow file).
- `04-02-SUMMARY.md` contains fixture table, guard one-liner, anti-flake notes, INT-03 E2E or deferral reference.
</verification>

<success_criteria>
- QA-02 satisfied with documented and passing local/CI E2E coverage.
</success_criteria>

<output>
After completion, ensure `.planning/phases/04-verification-docs/04-02-SUMMARY.md` is committed with the phase work (Task 5 produces it).
</output>
