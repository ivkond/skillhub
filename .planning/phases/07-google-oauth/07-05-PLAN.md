---
phase: 07-google-oauth
plan: "05"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - web/src/shared/components/language-switcher.test.ts
  - web/src/shared/theme/theme-code-surface-token.test.ts
autonomous: true
gap_closure: true
requirements:
  - QA-02
  - OAUTH-05
must_haves:
  truths:
    - "Web phase gate (`pnpm run typecheck`) passes without unrelated failures."
    - "Google OAuth login tests remain green after type-level fixes."
  artifacts:
    - path: web/src/shared/components/language-switcher.test.ts
      provides: "Type-safe test doubles for dropdown components under strict TS checks."
      contains: "ReactNode"
    - path: web/src/shared/theme/theme-code-surface-token.test.ts
      provides: "Typecheck-compatible CSS token test file IO import."
      contains: "readFileSync"
  key_links:
    - from: web/src/shared/components/language-switcher.test.ts
      to: web/tsconfig*.json
      via: "Strict typecheck over test sources"
    - from: web/src/shared/theme/theme-code-surface-token.test.ts
      to: web/src/features/auth/login-button.test.ts
      via: "Shared `pnpm run typecheck` gate for phase closeout"
---

# Phase 07 Plan 05: Gap Closure — Web Typecheck Gate

<objective>
Закрыть gap #2 из `07-VERIFICATION.md`: убрать unrelated typecheck failures в web-тестах, сохранив Google OAuth login behavior и backend-driven provider rendering (OAuth-05, QA-02).
</objective>

<context>
@.planning/phases/07-google-oauth/07-VERIFICATION.md
@.planning/phases/07-google-oauth/07-VALIDATION.md
@.planning/phases/07-google-oauth/07-02-SUMMARY.md
@web/src/shared/components/language-switcher.test.ts
@web/src/shared/theme/theme-code-surface-token.test.ts
@web/src/features/auth/login-button.test.ts
@web/src/pages/login.test.tsx
</context>

<threat_model>
<asvs_level>1</asvs_level>
<threats>
  <threat id="T-07-05-01" severity="high">Red web gate hides OAuth UI regressions and blocks secure release evidence.</threat>
  <threat id="T-07-05-02" severity="medium">Type fixes in shared tests can accidentally weaken UI quality assertions.</threat>
</threats>
<mitigations>
  <item>Ограничить изменения только двумя unrelated files из gap #2.</item>
  <item>После фикса прогнать typecheck + OAuth login tests в одном verify блоке.</item>
  <item>Запрещено удалять assertions, проверяющие provider rendering/redirect behavior.</item>
</mitigations>
<block_on>high</block_on>
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Воспроизвести и зафиксировать web typecheck RED из gap #2</name>
  <files>
    web/src/shared/components/language-switcher.test.ts
    web/src/shared/theme/theme-code-surface-token.test.ts
  </files>
  <read_first>
    @.planning/phases/07-google-oauth/07-VERIFICATION.md
    @web/src/shared/components/language-switcher.test.ts
    @web/src/shared/theme/theme-code-surface-token.test.ts
  </read_first>
  <behavior>
    - Test 1 (QA-02): `pnpm run typecheck` должен падать именно на двух files из gap #2 в RED состоянии.
    - Test 2 (OAuth-05): OAuth UI tests (`login-button`, `login`) не являются источником typecheck failure.
  </behavior>
  <action>
    Перед любыми правками символов в этом плане выполнить impact analysis:
    `gitnexus_impact({target: "LanguageSwitcher", direction: "upstream"})` и
    `gitnexus_impact({target: "theme-code-surface-token", direction: "upstream"})`;
    при HIGH/CRITICAL risk остановиться и зафиксировать blast radius.
    Запустить `cd web && pnpm run typecheck` и подтвердить, что ошибки относятся к:
    - `language-switcher.test.ts` (typing of mocked children/props),
    - `theme-code-surface-token.test.ts` (file-system import/typing compatibility).
    Не менять конфигурацию typecheck gate и не исключать тест-файлы из tsconfig.
  </action>
  <acceptance_criteria>
    - RED воспроизведен штатной gate-командой.
    - Ошибки локализованы в двух указанных файлах.
    - Scope фикса ограничен этими файлами.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[gitnexus_impact({target: "LanguageSwitcher", direction: "upstream"})]]></automated>
    <automated><![CDATA[gitnexus_impact({target: "theme-code-surface-token", direction: "upstream"})]]></automated>
    <automated><![CDATA[cd web && pnpm run typecheck]]></automated>
  </verify>
  <done>Имеется точная фиксация typecheck-break для минимального исправления gap #2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Исправить строгую типизацию тестов и подтвердить green gate</name>
  <files>
    web/src/shared/components/language-switcher.test.ts
    web/src/shared/theme/theme-code-surface-token.test.ts
  </files>
  <read_first>
    @web/src/shared/components/language-switcher.test.ts
    @web/src/shared/theme/theme-code-surface-token.test.ts
    @web/src/features/auth/login-button.test.ts
    @web/src/pages/login.test.tsx
  </read_first>
  <behavior>
    - Test 1 (QA-02): `pnpm run typecheck` проходит green.
    - Test 2 (OAuth-05): Google provider UI regression tests остаются green.
  </behavior>
  <action>
    Внести минимальные type-safe правки:
    - В `language-switcher.test.ts` заменить `unknown` children/props типы на корректные `ReactNode`/функциональные сигнатуры для mocked dropdown components.
    - В `theme-code-surface-token.test.ts` привести file IO import к формату, совместимому с текущим typecheck профилем проекта.
    - Сохранить все бизнес-assertions, связанные с semantic classes/token behavior и без ослабления expectations.
  </action>
  <acceptance_criteria>
    - Обе проблемы typecheck устранены без изменения test intent.
    - OAuth login tests (`login-button`, `login`) проходят после фикса.
    - Изменены только файлы из `files_modified`.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[cd web && pnpm run typecheck]]></automated>
    <automated><![CDATA[cd web && pnpm run test -- src/features/auth/login-button.test.ts src/pages/login.test.tsx]]></automated>
  </verify>
  <done>Gap #2 закрыт: web verification gate green и OAuth-UX regression coverage сохранен.</done>
</task>
</tasks>

<verification>
- `cd web && pnpm run typecheck`
- `cd web && pnpm run test -- src/features/auth/login-button.test.ts src/pages/login.test.tsx`
</verification>

<success_criteria>
- Gap #2 из `07-VERIFICATION.md` закрыт подтвержденным green typecheck gate.
- Google OAuth login rendering/redirect behavior не регрессировал.
</success_criteria>

<output>
After completion, create `.planning/phases/07-google-oauth/07-05-SUMMARY.md`
</output>
